{
  "name": "@bandmap/web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@bandmap/shared": "*"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
