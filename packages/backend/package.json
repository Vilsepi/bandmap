{
  "name": "@bandmap/backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/handler.js",
  "types": "./dist/handler.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "NODE_ENV=test node --import tsx --test src/**/*.test.ts",
    "test:integration": "node --import tsx --test ../../integration-tests/**/*.integration.ts"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1005.0",
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.1005.0",
    "@bandmap/shared": "*",
    "aws-jwt-verify": "^5.1.1",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "tsx": "^4.21.0"
  }
}
